import { Card, CardHeader, CardTitle } from "@unidash/components/Card";

export function EmptyIndicatorsCard() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Não possui dados</CardTitle>
      </CardHeader>
    </Card>
  );
}
