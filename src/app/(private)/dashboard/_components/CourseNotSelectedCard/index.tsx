import { Card, CardHeader, CardTitle } from "@unidash/components/Card";

export function CourseNotSelectedCard() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Selecione um curso</CardTitle>
      </CardHeader>
    </Card>
  );
}
